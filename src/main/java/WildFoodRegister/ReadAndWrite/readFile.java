package WildFoodRegister.ReadAndWrite;

public class readFile {
/*
    public static Produkt nacti(){
        try {
            File myObj = new File("src/main/java/dev/thatismybad/starter/openjfx/controller/nacitani.txt");
            Scanner myReader = new Scanner(myObj);
            while (myReader.hasNextLine()) {
                String data = myReader.nextLine();
                String [] pole =    data.split(";");
                int A = Integer.parseInt(pole[0]);
                int V = Integer.parseInt(pole[1]);
                double vlozenyObjem = Double.parseDouble(pole[2]);
                boolean kontrola =Boolean.parseBoolean (pole[3]);

                Produkt nactenyProdukt = new Produkt(A,V,vlozenyObjem,kontrola);

                System.out.println(data);
                return nactenyProdukt;
            }
            myReader.close();
        } catch (FileNotFoundException e) {
            System.out.println("An error occurred.");
            e.printStackTrace();
        }
        return null;

 */
    }








